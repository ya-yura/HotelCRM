{
  "name": "@hotel-crm/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "db:migrate": "tsx src/scripts/migrate.ts",
    "db:seed:json": "tsx src/scripts/seedFromJson.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@hotel-crm/shared": "0.1.0",
    "fastify": "^5.1.0",
    "pg": "^8.20.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.20.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
